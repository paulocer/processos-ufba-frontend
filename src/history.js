/**
 * Componente responsável por armazenar o historico de navegação
 * Utilizado como auxilio nos redirects.
 */
import createHistory from 'history/createBrowserHistory'

export default createHistory()